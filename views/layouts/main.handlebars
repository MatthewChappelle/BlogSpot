<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BlogSpot</title>

  <link rel="stylesheet" href="/css/style.css">
  <!-- Tailwind CSS -->
  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
</head>

<body id="profile" class="font-sans antialiased leading-normal tracking-wider bg-gray-900">
  <div class="flex flex-col justify-between">
    <header class="flex justify-between items-center p-2 sticky-header">
      <h1>
        <span role="img" aria-label="blog" class="text-3xl font-semibold text-gray-800">📝</span>
        <a href="/" class="text-3xl font-semibold text-green-700">BlogSpot</i></a>
      </h1>
      <nav class="flex items-center">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" class="text-green-700 hyperlink"><b>Profile</b></a> &nbsp; &nbsp;
        <button class="bg-green-700 hover:bg-green-900 text-gray-100 font-bold py-2 px-4 rounded-full"
          id="logout">Logout
        </button> &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
        {{else}}
        <a href="/login" class="text-green-700"><b>Login</b> &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; </a>
        {{/if}}
        <!-- Pin to top right corner -->
        <div class="absolute top-0 right-0 h-12 w-18 p-4">
          <button class="js-change-theme focus:outline-none"></button>
        </div>
      </nav>
    </header>
    <div class="flex justify-center items-center">
      <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
      </main>
    </div>
    <footer>
      <!-- Add your footer content here -->
    </footer>
  </div>


  <script src="https://unpkg.com/popper.js@1/dist/umd/popper.min.js"></script>
  <script src="https://unpkg.com/tippy.js@4"></script>
  <script>
    //Init tooltips
    tippy('.link', {
      placement: 'bottom'
    })




  </script>
  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>


</html>